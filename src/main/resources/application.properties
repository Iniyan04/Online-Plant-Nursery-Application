# Server
server.port=8080

# This project manages JPA/Hibernate itself via persistence.xml + JPAUtil
# (see com.nursery.util.JPAUtil), not Spring Data JPA auto-configuration.
spring.jpa.open-in-view=false
